using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleAppLogMetodos
{
    public class PersonRepository
    {
        public Person GetById(int Idperson)
        {
            return new Person();
        }

        public void Save(Person person)
        {
            //Save to db
        }

        public Person Get(string name, int id)
        {
            return new Person();
        }


    }
}
